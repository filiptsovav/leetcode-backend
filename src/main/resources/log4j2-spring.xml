<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
  <Appenders>
    <!-- Console appender -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1} - %msg%n"/>
    </Console>

    <!-- Rolling File appender -->
    <RollingFile name="FileAppender"
                 fileName="log/log.txt"
                 filePattern="log/log-%d{yyyy-MM-dd}-%i.txt.gz">
      <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c - %msg%n"/>
      <Policies>
        <!-- Ежедневная ротация -->
        <TimeBasedTriggeringPolicy/>
        <!-- И по размеру -->
        <SizeBasedTriggeringPolicy size="10MB"/>
      </Policies>
      <DefaultRolloverStrategy max="30"/>
    </RollingFile>
  </Appenders>

  <Loggers>
    <!-- Корневой логгер -->
    <Root level="info">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileAppender"/>
    </Root>

    <!-- Пример менее/более подробного логирования для пакетов -->
    <Logger name="com.example.demo.controller" level="debug" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileAppender"/>
    </Logger>

    <Logger name="com.example.demo.service" level="debug" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileAppender"/>
    </Logger>

    <Logger name="org.springframework" level="info" additivity="false">
      <AppenderRef ref="Console"/>
      <AppenderRef ref="FileAppender"/>
    </Logger>
  </Loggers>
</Configuration>
